{
  "name": "blockEncryptions",
  "version": "1.0.0",
  "description": "A set of helpers for block encryption",
  "main": "index.js",
  "scripts": {
    "test": "npx mocha test/",
    "testmon": "nodemon -w src -w test --exec 'npx mocha test/ || exit 1'"
  },
  "author": "Hamid Zare",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "bitwise-rotation": "^1.0.1"
  }
}
